	POLL_FDS
- Fonction qui decale les entrees de poll_fds a chaque remove pour ne pas avoir de trous
- fonction qui remove des clients si _count > 255
- depuis accept new connection (pour l'instant ca quitte sans leak ni pb)

	REDIRECTION POST AND DELETE
- a decider 
	--> ok, 301/302 sans effectuer l'action

	REQUEST CHECKING / parsing
- host header must be accurate : ip or ip+port or name or name+port
				only one host header, not empty
		--> ok with checkhostheader

	POST
- pour ne pas ecraser les fichiers ou psot sur un dir, + check que le chemin existe
--> acces(URL) et opendir(URL) doivent return false
--> std::string dir(URL.substr(URL.rfind('/'), URL.size()-1))
--> opendir(dir) doit marcher
	--> ok
- check max body size after content-len==realbodysize
	--> ok

	RQ_DIR
- href de create dir des sous dossiers/fichiers pb
	--> ok via user_path 

	LIST_SERV_FD / CHECK_SERV_SOCK
- invalid read lie a l'init?
	--> ok (checkservsock avec nb de serv sock, pas getcount)

	CHRCK_CGI_EXT
- invalid read avec GET 127.0.0.1:8080/site1/somedir 
	--> ok : si pas de find('.') return false direct

	PARSIING CONFIGFILE
- les deux accolades meme ligne --> genere un autre erreur
- dernier serveur du config sans accolade fermante : ca passe
	--> ok
- localhost as 127.0.0.1
	--> ok
- empty config file (only empty lines or nothing)
	--> ok
- server with no location
	--> ok on return 404

	TRY CATCH EXEC_RQ
- utile?
	--> nope, supp

	CGI PATHES
- a virer de configfile.cpp +
